## TODO

- [ ] Add `.gitignore` (nothing currently tracked that needs ignoring, but the universal floor expects one — `.DS_Store`, editor swap files, `*.log` would be reasonable defaults).
- [ ] Fix `README.MD` typo: "It is crezy how simple solutions are often the best." → "crazy". Came in via remote `bada6e4 small changes` (2025-05-23), still present.
